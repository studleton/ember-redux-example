<footer>
  Filters:
  <Filter @filter='SHOW_ALL' @visibilityFilter={{this.visibilityFilter}}>
    All
  </Filter>
  <Filter @filter='SHOW_ACTIVE' @visibilityFilter={{this.visibilityFilter}}>
    Active
  </Filter>
  <Filter @filter='SHOW_COMPLETED' @visibilityFilter={{this.visibilityFilter}}>
    Completed
  </Filter>
</footer>